{
  "name": "coopart",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ coopart",
    "create-local": "graph create --node http://localhost:8020/ coopart",
    "remove-local": "graph remove --node http://localhost:8020/ coopart",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 coopart"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
